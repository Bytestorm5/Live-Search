# Dependencies
node_modules/
.pnp
.pnp.js

# Build output
dist/
dist-ssr/
*.local

# Test / coverage
coverage/
.vitest/

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment
.env
.env.*
!.env.example

# Generated documentation index (produced by `npm run ingest`).
# Keep the raw corpus in docs-corpus/ under version control; the built index
# is a derived artifact. Comment this out if you prefer to commit the prebuilt
# index for zero-build static hosting.
public/corpus.index.json

# Downloaded model weights cached locally during development
.models/
# Self-hosted model assets fetched by `npm run setup:assets` (derived artifacts)
public/models/
